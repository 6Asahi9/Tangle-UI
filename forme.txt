i have an app which has a graphicviewcanvas basically the scene with some override functions  
a canvasnode which are the attritubtes of the treewidget items 
treewidget is also connected to the toolbox folder so if something is like toolbox/test/testing.txt , treewidget will show them as test/testing which is draggable and gets accepted by the scene 
also has a node.h which is the identity of each node (tree widget item) nodelist.h (for later when there are multiple nodes)
and a startupwindow which asks user to make a folder in that folder we makes (model.py, view.json and node.ptr)
obviously i have these files 
mainwindow.h , cpp and header 
GraphicViewCanvas.h , cpp
CanvasNode.h , cpp
StartupWindow.h , cpp
Node.h and NodeList.h

the mainpurpose of this app is drag some item from treewidget in the scene ->(THIS part is done) now i want that item location (x,y) gets saved in node.ptr and that project gets saved in a "treewidget_2" basically its name no the whole path (root folder has a projects.json file in it)